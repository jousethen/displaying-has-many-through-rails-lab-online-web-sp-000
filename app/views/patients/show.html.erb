<h2><%= @patient.name %></h2>
<h3>Age: <%= @patient.age%> </h3>

Appointments:
<ul>
  <% @patient.appointments.each do |a| %>
    <li>
      <%= a.appointment_datetime.strftime("%B %d, %Y at %k:%M")%> - with Dr. <%= link_to a.doctor.name, doctor_path(a.doctor.id) %>
    </li>
  <% end %>
</ul>